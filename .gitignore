# project-4 is its own throwaway repo (needs a real GitHub remote to open PRs against)
project-4-fix-loop/

# project-5 is its own throwaway repo too (opens PRs, same reason as project-4)
project-5-codify-body/

# project-6 is its own throwaway repo too (GitHub PR-trigger routine needs a real connected repo)
project-6-doorbell-loop/

# project-9 is its own throwaway repo too (a Routine needs a real
# connected GitHub repo to clone and fire one-off runs against)
project-9-rehearse-routine/

# project-10 is its own throwaway repo too (a Routine needs a real
# connected GitHub repo to clone and fire one-off runs against)
project-10-secrets-drill/
